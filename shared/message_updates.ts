export const success = (message: string): string => `✅ ${message}`

export const failure = (message: string): string => `❌ ${message}`

export const location = (message: string): string => `📦 ${message}`

export const book = (message: string): string => `📘 ${message}`

export const author = (message: string): string => `🧑 ${message}`
